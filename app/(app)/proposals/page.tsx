import { Placeholder } from "@/components/placeholder";

// Step 7 — proposals are generated from briefs (/proposals/:id). List surface TBD.
export default function ProposalsPage() {
  return <Placeholder title="Proposals" step={7} />;
}
