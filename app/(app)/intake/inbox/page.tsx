import { Placeholder } from "@/components/placeholder";

// Step 12 — /intake/inbox: threaded sources
export default function IntakeInboxPage() {
  return <Placeholder title="Intake Inbox" step={12} />;
}
