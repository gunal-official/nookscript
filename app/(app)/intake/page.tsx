import { Placeholder } from "@/components/placeholder";

// Step 4 — /intake: paste source, AI brief generation, gap flagging
export default function IntakePage() {
  return <Placeholder title="Intake" step={4} />;
}
