import { Placeholder } from "@/components/placeholder";

// Step 8 — task plans live at /plans/:id. List surface TBD.
export default function PlansPage() {
  return <Placeholder title="Plans" step={8} />;
}
