import { Placeholder } from "@/components/placeholder";

// Step 9 — client updates live at /updates/:id. List surface TBD.
export default function UpdatesPage() {
  return <Placeholder title="Updates" step={9} />;
}
